# Public-safe template for admin-managed SMTP secrets.
# Copy to ~/.config/servertool/smtp.env and replace placeholders.

export SERVERTOOL_SMTP_USERNAME=notify@example.com
export SERVERTOOL_SMTP_PASSWORD=replace-with-real-password
